# 选股 DSL 语法帮助文档

本文件是「行情选股 DSL」的完整准确语法参考，对应实现在 [dsl_engine.py](dsl_engine.py)。DSL 经五阶段编译器（词法 → 语法 → 语义 → SQL 生成 → 执行）翻译为 SQL，在 DuckDB / Doris 双后端执行，结果一致。

> 一句话：用「字段 运算符 阈值」写一条件，用 `AND` / `OR` / `NOT` 组合，文本常量用英文双引号括起。整个表达式的最终结果**必须是条件（布尔值）**，例如 `收盘价 > 10`，不能写成裸数 `MA(收盘价,5)`。

---

## 一、词法基础

| 元素                  | 写法                | 说明                                  |
| --------------------- | ------------------- | ------------------------------------- |
| 数值                  | `5` `0.98` `-3`     | 整数或浮点；词法层 `-` 与数字分开，语法层 `parse_unary` 组合 |
| 文本常量              | `"银行"` `'600519'` | 单引号或双引号均可；不支持转义        |
| 标识符（字段/函数名） | 中文或英文字母开头  | 支持中文（一-鿿）、字母、数字、下划线 |
| `*ST` 字段 | `*ST` `*ST股` | 专用词法分支识别，`*` 不会被当乘法 |
| 字符串括号            | `( )`               | 改变优先级 / 函数参数列表             |

不区分大小写的关键字：`AND`/`与`/`&&`、`OR`/`或`/`||`、`NOT`/`非`/`!`、`IN`、`包含`。

---

## 二、字段词库

所有可用字段由引擎内置字典定义，用户不能自造字段名。

### 2.1 数值型字段（来自「当日信息表」最新交易日）

| 字段名       | 单位     | 示例                                     |
| ------------ | -------- | ---------------------------------------- |
| 开盘价       | 元       | `开盘价 > 5`                             |
| 收盘价       | 元       | `收盘价 <= 20`                           |
| 最高价       | 元       | `最高价 >= 收盘价`                       |
| 最低价       | 元       | `最低价 >= N日最低(60)`                  |
| 成交量       | 手       | `成交量 > 100000`                        |
| 成交额       | 万元     | `成交额 > 30000`                         |
| 涨跌幅       | %        | `涨跌幅 > 5`                             |
| 涨跌额       | 元       | `涨跌额 > 0.5`                           |
| 振幅         | %        | `振幅 > 3`                               |
| 换手率       | %        | `换手率 > 5`                             |
| 市盈率       | 倍       | `市盈率 < 20 AND 市盈率 > 0`             |
| 动态市盈率   | 倍       | `动态市盈率` → 同 `市盈率`（指向 pe 列） |
| 市盈率TTM    | 倍       | —                                        |
| 市净率       | 倍       | `市净率 < 1`                             |
| 市销率       | 倍       | —                                        |
| 市销率TTM    | 倍       | —                                        |
| **总市值**   | **亿元** | `总市值 > 2000`（库中万元已自动 /10000） |
| **流通市值** | **亿元** | `流通市值 > 500`                         |
| 总股本       | 万股     | —                                        |
| 流通股本     | 万股     | —                                        |
| 股息率       | %        | `股息率 > 2`                             |
| 股息率TTM    | %        | —                                        |
| 量比         | -        | `量比 > 2`                               |

> **单位注意**：总市值 / 流通市值在 DSL 中已换算为「亿元」，成交额为「万元」，成交量为「手」。

### 2.2 文本型字段（来自「基本信息表」）

| 字段名 | 别名     | 说明                                            |
| ------ | -------- | ----------------------------------------------- |
| 名称   | 股票名称 | 股票简称，配合 `包含` 做关键字筛选              |
| 代码   | 股票代码 | 6 位代码，如 `"600519"`                         |
| 行业   | -        | 如 `"银行"` `"白酒"`，可 `IN` 多值              |
| 地域   | -        | 省份，如 `"广东"` `"北京"`                      |
| 板块   | -        | `"科创板"` `"创业板"` `"上证主板"` `"深证主板"` |
| 交易所 | -        | 见下方别名映射                                  |

**交易所别名自动映射**（比较 / IN 时自动转换）：

| 中文别名               | → 库内代码 |
| ---------------------- | ---------- |
| `上证` / `上海` / `沪` | `SSE`      |
| `深证` / `深圳` / `深` | `SZSE`     |
| `北证` / `北京`        | `BSE`      |

写 `交易所 = "上证"` 等价于 `交易所 = "SSE"`。

### 2.3 布尔型字段（风险警示）

| 字段名                     | 语义                              | 生成 SQL            |
| -------------------------- | --------------------------------- | ------------------- |
| `ST` / `ST股` / `风险警示` | **含 ST**（含纯 ST 与 *ST，原义） | `name LIKE '%ST%'`  |
| `*ST` / `*ST股`            | **纯 \*ST**                       | `name LIKE '%*ST%'` |

布尔字段只能用 `=` / `!=` / `NOT`，**不支持** `<` `>` `>=` `<=` 比较。`=` 右侧可写布尔字面量 `true` / `false`（如 `ST = false` 等价 `NOT ST`）。

利用两者的精度区分三种股票：

| 目标                             | 写法                   |
| -------------------------------- | ---------------------- |
| 纯 ST 股（含 ST 但不含 *ST）     | `ST AND NOT *ST`       |
| 纯 *ST 股                        | `*ST`                  |
| 全部风险警示股（ST + *ST）       | `ST`                   |
| 非风险警示股                     | `NOT ST`               |
| 排除纯 *ST（保留正常股 + 纯 ST） | `NOT *ST`              |
| 排除纯 ST（保留正常股 + 纯 *ST） | `NOT (ST AND NOT *ST)` |

向后兼容：现有用 `ST` 的 DSL 语句含义不变。

---

## 三、历史函数（来自「日度信息表」时序聚合）

历史函数基于日度信息表按交易日回溯 N 天，**N 上限 250**。内部通过 `ROW_NUMBER() PARTITION BY ts_code ORDER BY trade_date DESC` 实现。

| 函数签名                         | 说明                                                              | 示例                             |
| -------------------------------- | ----------------------------------------------------------------- | -------------------------------- |
| `MA(指标, N)`                    | N 日移动平均。指标可取：收盘价/开盘价/最高价/最低价/成交量/成交额 | `MA(收盘价, 20)`                 |
| `区间涨幅(N)` （别名 `N日涨幅`） | 最新收盘价相对 N 个交易日前的涨跌幅（%）                          | `区间涨幅(20) > 15`              |
| `N日最高(N)`                     | 最近 N 日最高价的最大值                                           | `收盘价 >= N日最高(60) * 0.98`   |
| `N日最低(N)`                     | 最近 N 日最低价的最小值                                           | `收盘价 <= N日最低(20)`          |
| `连涨(N)`                        | 最近 N 个交易日涨跌幅全部 > 0（不足 N 日或含空值日按实际有效交易日判定） | `连涨(3) AND 换手率 > 5`         |
| `连跌(N)`                        | 最近 N 个交易日涨跌幅全部 < 0（不足 N 日或含空值日按实际有效交易日判定） | `连跌(5) AND 市盈率 > 0`         |
| `创新高(N)`                      | 最新收盘价达到最近 N 日最高值                                     | `创新高(120) AND 成交额 > 30000` |
| `创新低(N)`                      | 最新收盘价跌至最近 N 日最低值                                     | `创新低(120)`                    |

返回值是数值或布尔（`连涨`/`连跌`/`创新高`/`创新低` 返回布尔，可直接作条件）。

---

## 四、运算符与优先级

按从低到高（低优先级先结合）：

| 优先级    | 运算符                              | 说明                                                     |
| --------- | ----------------------------------- | -------------------------------------------------------- |
| 1（最低） | `OR` / `或` / `\|\|` | 逻辑或 |
| 2         | `AND` / `与` / `&&`                 | 逻辑与                                                   |
| 3         | `NOT` / `非` / `!`                  | 逻辑非（一元，右结合）                                   |
| 4         | 比较与谓词                          | `=` `==` `!=` `<>` `>` `>=` `<` `<=`；`IN (...)`；`包含` |
| 5         | `+` `-`                             | 加减                                                     |
| 6         | `*` `/`                             | 乘除（除法自动 `NULLIF` 防除零）                         |
| 7（最高） | 一元 `-` / 括号 / 函数调用 / 字面量 | 原子                                                     |

- `==` 与 `=` 等价；`<>` 与 `!=` 等价。
- `包含` 关键字中的 `%` 和 `_` 会被忽略（防 LIKE 通配符注入）：`名称 包含 "50%"` 实际搜索 `50`。
- 算术两边都必须是数值；`/` 生成 `NULLIF(右值, 0)`，除以零返回 NULL（该支股票被排除）。
- 括号可任意改变结合顺序：`(行业 = "银行" OR 行业 = "证券") AND 市盈率 < 8`。

---

## 五、SQL 生成与方言

DSL 编译后生成一条 SQL：基础查询为「基本信息表 JOIN 当日信息表（最新交易日）」，WHERE 子句由 DSL 条件拼接，按 `symbol` 排序，默认 `LIMIT 10000`。

方言差异由 `Dialect` 类与 `db.py` 自动处理，用户无需感知：

| 维度       | DuckDB 方言              | Doris 方言                           |
| ---------- | ------------------------ | ------------------------------------ |
| 表名       | 中文表名（`当日信息表`） | 英文表名（`daily_basic`）            |
| 引号       | 双引号 `"col"`           | 反引号 `` `col` ``                   |
| 字面量处理 | 单引号转义后内联         | 单引号 + 反斜杠双重转义后内联        |
| 字符串拼接 | `\|\|`                   | `CONCAT()`                           |
| 保留字     | 无需处理                 | `open`/`close`/`change` 等自动反引号 |

> 注：DSL 引擎生成的字面量经方言层转义后内联进 SQL，不依赖数据库端占位符（`db.py` 层在旧裸 SQL 通路中按需使用占位符）。

`execute_dsl()` 的 dialect 未指定时从 `db.DB_TYPE`（环境变量 `STOCK_DB_TYPE`）自动推断。

---

## 六、返回结果

执行返回与表单筛选同构的行数据，每行字段：

```text
证券种类 | 证券名称 | 证券代码 | 交易所码 | 交易板块 |
最近收盘价 | 动态市盈率 | 市净率 | 总市值（亿元） | 流通市值（亿元）
```

- 总市值 / 流通市值在返回时已由「万元」换算为「亿元」并保留 2 位小数。
- 条件引用某指标时，该指标为空的股票会被排除（如 `市盈率 < 20` 会排除市盈率为空的股票）；不被条件引用的空值不影响入选，结果中显示为 `None`（如无条件查询会返回市盈率为空的股票）。

---

## 七、典型示例

| 场景           | DSL                                                                  |
| -------------- | -------------------------------------------------------------------- |
| 低估值银行     | `行业 = "银行" AND 市盈率 < 8 AND 市净率 < 1`                        |
| 均线多头排列   | `MA(收盘价, 5) > MA(收盘价, 20) AND MA(收盘价, 20) > MA(收盘价, 60)` |
| 近期强势股     | `区间涨幅(20) > 20 AND 换手率 > 3 AND NOT ST`                        |
| 突破创新高     | `创新高(120) AND 成交额 > 30000 AND 非 ST`                           |
| 连跌后观察     | `连跌(5) AND 市盈率 > 0 AND 市盈率 < 20`                             |
| 大盘蓝筹高股息 | `总市值 > 2000 AND 股息率 > 2`                                       |
| 金融板块集合   | `行业 IN ("银行", "证券", "保险") AND 地域 = "上海"`                 |
| 名称关键字     | `名称 包含 "科技" AND 收盘价 < 30`                                   |
| 纯 *ST 观察池  | `*ST AND 换手率 > 1`                                                 |
| 接近新高回踩   | `收盘价 >= N日最高(60) * 0.98 AND NOT 创新高(5)`                     |

---

## 八、常见错误与规避

| 错误                                | 原因                        | 修法                                             |
| ----------------------------------- | --------------------------- | ------------------------------------------------ |
| `这里不应该出现 '*'`                | 旧版词法不识别 `*ST`        | 已修复；`*ST`（含别名 `*ST股`）作字段时合法，孤立 `*` 仍是乘法 |
| `整个表达式的结果必须是条件`        | 写成裸数值如 `MA(收盘价,5)` | 必须用比较包成条件：`MA(收盘价,5) > 10`          |
| `条件（布尔值）只支持 = 与 != 比较` | 对 `ST` 写了 `ST > 0`       | 布尔字段只能 `ST`、`NOT ST`、`ST = true` / `ST = false` |
| `比较两边类型不一致`                | 如 `行业 = 5`               | 文本字段配文本常量，数值字段配数值               |
| `包含 两边都必须是文本`             | 如 `收盘价 包含 "5"`        | `包含` 仅用于 `名称` 等文本字段                  |
| `IN 列表里的值类型应均为…`          | IN 列表混入异类型           | `行业 IN ("银行", "证券")` 全用文本              |
| `不认识的字段 'XX'`                 | 拼错字段名                  | 参见本文第二节词库                               |
| `历史函数 N 上限 250`               | 如 `MA(收盘价, 300)`        | N ≤ 250                                          |

---

## 九、与表单筛选的关系

表单筛选（`/api/filter`）的后端会把表单各选择维度翻译为一段 DSL 文本，调本引擎执行，返回字段不变；DSL 面板（`/api/dsl_filter`）直接接收用户输入的 DSL。两条通路共享同一编译器与数据库，结果一致。

| 表单维度   | DSL 翻译                                                                          |
| ---------- | --------------------------------------------------------------------------------- |
| 交易所板块 | `交易所 IN ("SSE","SZSE","BSE")` / `板块 IN ("创业板","科创板",...)`，两类用 `OR` |
| 行业       | `行业 IN ("银行","证券",...)`                                                     |
| 地域       | `地域 IN ("广东","北京",...)`                                                     |
| 风险警示   | 见 2.3 节 7 种组合映射                                                            |
| 名称关键字 | `名称 包含 "kw"`，多关键字用 `OR`                                                 |
| 指标范围   | `市盈率 <= 20`、`总市值 >= 100`（亿元）等                                         |
| 概念板块   | 数据库无 concept 列，**静默忽略**（表单与 DSL 均不支持）                          |

> 行为说明：表单筛选改走 DSL 引擎后（2026-07-30），两处行为不同于旧 pandas 通路——条件匹配 0 只时返回空表（旧逻辑会跳过该条件返回全部）；名称关键字按字面量匹配（旧逻辑按正则，含 `.` `*` 的关键字结果可能不同）。未识别的枚举值会写入返回日志（旧逻辑静默忽略）。

---

## 附：词库来源

本文档字段、运算符、函数均来自 [dsl_engine.py](dsl_engine.py) 的：

- `NUMERIC_FIELDS`（第 51 行）、`STRING_FIELDS`（第 78 行）、`BOOL_FIELDS`（第 90 行）
- `EXCHANGE_ALIASES`（第 99 行）、`MA_FIELDS`（第 107 行）、`HIST_FUNC_DESCS`（第 112 行）
- `_TOKEN_RE`（第 168 行）、`_Parser`（第 220 行）、`_Generator`（第 363 行）
- `get_dsl_help()`（第 711 行）
