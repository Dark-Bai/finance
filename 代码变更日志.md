# 代码变更日志（03a7c87 起）

> 本文件记录从 `03a7c87` 起的**代码级变更**（代码修改、功能新增、架构调整），
> 不含文字型文档修改（排查方案、架构文档、checkpoint 等）及纯样式调整。
> 
> 生成日期：2026-07-30

---

## 2026-07-29 — DuckDB → Doris 数据库层迁移

**commit `3ddd38e`** — `feat: DuckDB → Doris 数据库层迁移，支持 STOCK_DB_TYPE 环境变量切换`

涉及文件：`db.py`（新建）、`dsl_engine.py`、`app.py`

### 变更内容

**新建 `db.py` 抽象层**
- 新增 `db_connect()`：根据环境变量 `STOCK_DB_TYPE` 自动返回 DuckDB 连接或 Doris 连接池
- 新增 `fetchdf()`：将 Doris pymysql cursor 结果转为 pandas DataFrame，统一两后端返回格式
- 新增 `translate_table()` / `translate_placeholder()`：中文表名→英文表名映射、`?`→`%s` 占位符转换
- `TABLE_MAP`：5 张表中文→英文映射（`日度信息表`→`daily_kline` 等）
- Doris 侧使用 DBUtils `PooledDB`（最大 20 连接，最小 2 缓存，ping 自动重连）

**`dsl_engine.py` 方言层**
- `Dialect` 基类新增 `table_name()` / `concat()` 接口
- `DorisDialect` 覆写：表名走英文名、字符串拼接用 `CONCAT()`、字面量单引号+反斜杠双重转义
- `build_select()` 4 处硬编码中文表名改为 `self.d.table_name()`
- `_gen_contains` 字符串拼接从 `||` 改为 `self.d.concat()`
- `execute_dsl()` 改为从 `db.db_connect()` 取连接，DuckDB/Doris 双分支自动路由

**`app.py` 数据库调用点**
- `import duckdb` 改为 `import db`
- 3 处 `duckdb.connect(DB_PATH)` → `db_connect()`（双分支）
- 裸 SQL 中的 Doris 保留字 `open`/`close`/`change` 在 Doris 分支加反引号
- `execute_dsl()` 调用去掉 `db_path` 参数，dialect 随 `DB_TYPE` 自动推断

### 架构影响

数据库切换从"改多个文件多处的连接和 SQL"压缩为**改一个环境变量**。DSL 编译器与 Flask 业务逻辑不再依赖具体数据库实现。

---

## 2026-07-29 — DSL 方言修复（迁移后回归 bug）

**commit `38e038a`** — `fix: DSL 方言强制跟随 DB_TYPE，修复 Doris 模式下前端硬编码 duckdb 导致的语法错误`

涉及文件：`app.py`、`static/js/script.js`

### 问题

前端 `script.js` 在 POST body 中硬编码 `dialect: 'duckdb'`，后端据此生成中文表名+双引号的 DuckDB SQL，送 Doris 执行时报 `STRING LITERAL` 语法错误。

### 修复

- **后端** (`app.py` `/api/dsl_filter`)：dialect 强制取 `DB_TYPE`，忽略前端传值（执行后端由 DB_TYPE 决定，方言必须一致）
- **前端** (`script.js`)：POST body 不再传 `dialect: 'duckdb'`

### 验证

curl 测 `/api/dsl_filter`（`行业="银行" AND 市盈率<8`）→ `dialect: doris`，SQL 含 `daily_basic` 英文表名 ✅

---

## 2026-07-30 — 表单筛选 DSL 化大重构

**commit `561aa89`** — `refactor: 表单筛选改为 DSL 引擎执行，全部筛选条件下推数据库`

涉及文件：`app.py`、`dsl_engine.py`

### 变更内容

**`dsl_engine.py`**
- `BOOL_FIELDS` 新增 `*ST` / `*ST股`（纯 *ST，`LIKE '%*ST%'`），`ST` / `ST股` / `风险警示` 原语义（含 ST 和 *ST）不变，向后兼容
- `_TOKEN_RE` 新增 `STAR_ST` 分支识别 `*ST` 标识符（`\*ST股?`），避免 `*` 被解析为乘法

**`app.py`**
- 新增 `build_dsl_from_form_params()`：将表单各维度（交易所板块、行业、地域、风险警示、名称关键字、指标范围）翻译为 DSL 文本
- 新增 `execute_form_filter_as_dsl()`：调用 DSL 引擎编译执行
- 删除 `Main_Func_Get_securities_identification_data__` 内的两表取数 + 6 层 pandas 过滤（交易所/行业/地域/风险/名称关键字/指标）
- 删除 `Main_function_apply_metric_filter__`（指标过滤独立函数）
- `/api/filter` 改调 `execute_dsl()`，返回 JSON 形状不变对前端透明
- 风险警示 7 组合 DSL 映射：
  - 非风险 `NOT ST` / 纯 ST `ST AND NOT *ST` / *ST `*ST`
  - 非ST+ST `NOT *ST` / 非ST+*ST `NOT (ST AND NOT *ST)` / 全风险 `ST` / 全选省略

### 架构影响

筛选管道从"拉全量 → pandas 过滤 → 丢弃 99% 数据"改为"DSL 编译 → 一条 SQL 在数据库端执行"，性能与一致性大幅提升。两条通路（表单 `/api/filter` + DSL 面板 `/api/dsl_filter`）共享同一编译器，结果一致。

---

## 2026-07-30 — 6 处逻辑矛盾修复

**commit `438f018`** — `fix: 修复排查发现的 6 处逻辑矛盾`

涉及文件：`dsl_engine.py`、`app.py`、`update_db.py`

### 变更内容

**`dsl_engine.py`**
- `STAR_ST` 词法正则从 `\*ST` 改为 `\*ST股?`，修复 `*ST股` 别名死代码（原词法只匹配 `*ST`，`股` 被切成独立标识符）
- `_gen_field` 新增 `true` / `false` 布尔字面量处理（`ST = true` / `ST = false` 合法化）
- 新增 `_esc_str_lit()`：Doris（MySQL 协议）字符串反斜杠双倍转义，修复双后端行为分裂
- `get_dsl_help()` 补列动态市盈率字段、修正 NULL 排除表述、补充 `%` / `_` 通配符剥除说明

**`app.py`**
- `build_dsl_from_form_params` 返回 `warnings` 列表，未知板块 / 风险组合 / 指标写日志不再静默忽略
- `/api/filter` 新增 `DSLError` 捕获分支（返回 `error` + `error_pos`，与 `/api/dsl_filter` 一致）

**`update_db.py`**
- 基本信息表 / 当日信息表 `DELETE + INSERT` 包事务（`BEGIN` / `COMMIT` / `ROLLBACK`），INSERT 失败回滚防留空表

### 验证

DuckDB 执行回归 14/14；双方言编译回归 38/38。

---

## 2026-07-30 — Doris LIKE/= 优先级修复

**commit `920628b`** — `fix: BOOL_FIELDS 表达式加括号，修复 Doris 中 LIKE 与 = 优先级差异`

涉及文件：`dsl_engine.py`

### 问题

Doris 中 `=` 优先级高于 `LIKE`，`b.name LIKE '%ST%' = true` 被解析为 `b.name LIKE ('%ST%' = true)`，等价 `b.name LIKE FALSE`，`ST = true` / `ST = false` 在 Doris 恒返回空结果（DuckDB 正常）。

### 修复

`BOOL_FIELDS` 映射表达式加括号：`(b.name LIKE '%ST%')`，确保 `LIKE` 先于 `=` 求值。

### 验证

对拍发现后当场修复，20 条用例双后端行数与结果集哈希全部一致。

---

## 2026-07-30 — 4 项排查确认 bug 修复

**commit `5c0327d`** — `fix: 修复 4 项排查确认的 bug（死代码/迁移漏调/事务保护/退化分支）`

涉及文件：`app.py`、`migrate_to_doris.py`、`update_db.py`

### 变更内容

**`app.py`** — 删除死代码 `Tool_function_replace__`
- 函数体 4 处 `==` 误写（Python 中 `==` 是表达式不是赋值），但全项目 `grep` 仅发现 `def` 无任何 `call`，属已删除旧目录残留
- 整函数删除，0 风险

**`migrate_to_doris.py`** — `main()` 补调 `migrate_others(con)`
- `main()` 原有流程只迁移 `daily_kline` 和 `stock_basic`，当日信息表 / 周度信息表 / 月度信息表从未迁移
- `verify()` 读表计数时 3 张表为 0 行，数据不完整

**`update_db.py`** — `update_weekly_monthly` 事务保护
- 周度 / 月度信息表 `DELETE × 2 + INSERT × 2` 包进 `BEGIN` / `COMMIT` / `ROLLBACK`，任一失败回滚避免空表或两表不一致

**`update_db.py`** — `get_latest_trade_date_with_data` 退化分支
- 5 处 `break` 静默跳过 + 尾部 `return candidate` 改为 `raise Exception`，不再静默返回非交易日 / stale 日期

---

## 2026-07-30 — 代码层迁移目录整理

**commit `03a7c87`** — `chore: 按 main 分支目录结构整理根目录`

涉及文件：根目录及 `dsl-stock-screener/`

### 变更内容

- 删除根目录中已存在于 `竞赛文档/` 的 9 个重复文件
- 删除旧版 `股票分析智能体/` 目录（被 `dsl-stock-screener/` 完全取代）
- 迁移 `doris_schema.sql`、`migrate_to_doris.py` 到 `dsl-stock-screener/`
- 根目录从 17 项精简到 7 项

---

## 变更汇总

| 文件 | 变更多 | 主要变更类型 |
|---|---|---|
| `app.py` | 6 commits | bug 修复（死代码删除、方言修复）、大重构（表单 DSL 化）、Doris 连接切换 |
| `dsl_engine.py` | 5 commits | *ST 词法修复、布尔字面量、Doris 转义、LIKE/= 优先级、方言层新增 |
| `update_db.py` | 2 commits | 事务保护（基本信息表/当日表/周度表/月度表）、退化分支 raise |
| `migrate_to_doris.py` | 1 commit | main() 补调 migrate_others |
| `static/js/script.js` | 1 commit | 前端移除硬编码 dialect |
| `db.py` | 1 commit（新建） | DuckDB/Doris 双后端抽象层 |
| `migrate_to_doris.py` | 1 commit | 主流程补全遗漏的 3 张表迁移调用 |
| `rootcheckpoint.md` | 2 commits | 同步记录代码变更 |

### 时间线

```
3ddd38e  ── DuckDB → Doris 数据库层迁移（feat）
38e038a  ── DSL 方言修复（fix）
561aa89  ── 表单筛选 DSL 化重构（refactor，最大单次变更 -262/+374 行）
438f018  ── 6 处逻辑矛盾修复（fix）
920628b  ── Doris LIKE/= 优先级修复（fix）
5c0327d  ── 4 项排查确认 bug 修复（fix）
03a7c87  ── 目录整理（chore）
```
