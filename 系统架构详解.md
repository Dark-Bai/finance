# 系统架构详解

> **读者**：本项目面向竞赛评委、队友协作、系统维护者三类读者。
>
> 目录结构：
> - 第 1-5 章：零基础入门（什么是 DSL、数据从哪来、数据库长什么样）
> - 第 6-8 章：技术细节（DSL 编译流水线、Flask 路由、双后端抽象层）
> - 第 9-11 章：工程全景（前端页面、K 线管线、基本面管线）
> - 第 12-13 章：运维与 FAQ

---

## 目录

1. [一句话说清这个系统](#1-一句话说清这个系统是什么)
2. [先建立几个基础概念](#2-先建立几个基础概念)
3. [系统的五大组成部分](#3-系统的五大组成部分)
4. [第一部分：数据从哪里来——数据导入管道](#第一部分数据从哪里来数据导入管道)
5. [第二部分：数据存在哪里——数据库](#第二部分数据存在哪里数据库)
6. [第三部分：系统的核心大脑——DSL 翻译引擎](#第三部分系统的核心大脑dsl-翻译引擎)
7. [第四部分：系统的骨架——Flask 后端](#第四部分系统的骨架flask-后端)
8. [第五部分：用户看到的脸——前端页面](#第五部分用户看到的脸前端页面)
9. [两条筛选管道：薄 SQL 厚 Python vs 厚 SQL 薄 Python](#两条筛选管道薄-sql-厚-python-vs-厚-sql-薄-python)
10. [K 线详情与基本面管线](#k-线详情与基本面管线)
11. [一次完整的运行：从开机到出结果](#一次完整的运行从开机到出结果)
12. [数据库双后端架构](#数据库双后端架构)
13. [常见疑问解答](#常见疑问解答)
14. [附：技术栈速查表](#附技术栈速查表)

---

## 1. 一句话说清这个系统是什么

**这是一个"选股翻译官"系统。**

你用接近自然语言的表达式写一句选股要求：

```
行业 = "银行" AND 市盈率 < 8 AND MA(收盘价, 5) > MA(收盘价, 20)
```

系统自动把这句"人话"翻译成数据库能执行的 SQL，在存了 1800 万条股票数据的数据库里翻找，几秒钟后把符合条件的股票列表端到你面前。

**为什么需要"翻译"？**
因为人会说"市盈率小于8"，但数据库只认得 `pe < 8`（pe 是市盈率的英文物理列名）。中间必须有个翻译环节，让人不用背列名、不用学 SQL，也能高效筛选。

**它不是干什么的：**
- 它不是炒股软件，不会替你买卖
- 它不是实时行情软件（数据是定时批量拉取的快照）
- 它不是预测系统，不告诉你明天涨跌

**它的本质是：把"选股想法"变成"数据库查询"并执行。**

---

## 2. 先建立几个基础概念

### 2.1 前端 vs 后端

| 概念 | 比喻 | 在本系统 |
|---|---|---|
| **前端** | 餐厅的菜单和点菜屏 | 你在浏览器里看到的页面、按钮、表格 |
| **后端** | 后厨 | 你看不到的服务器程序，负责真正干活（查数据） |

你点菜（前端点按钮）→ 服务员传单（网络请求）→ 后厨做菜（后端查数据）→ 端上桌（结果回前端显示）。

### 2.2 数据库

想象一个超级大的 Excel 文件，但它：
- 不是一张表，而是**多张表**，表和表之间能"关联"
- 能在**几百万行**里**瞬间**找到你要的行
- 接受一种叫 **SQL** 的提问语言

本系统开发用 **DuckDB**（嵌入式，像打开一个文件一样就能用，单文件约 1.5GB），生产用 **Apache Doris**（分布式 OLAP，支持更大规模和并发查询）。

### 2.3 SQL

SQL（读作"S-Q-L"或"sequel"）是数据库的通用提问语言：

```sql
SELECT 名称, 市盈率 FROM 股票表 WHERE 市盈率 < 8
```

翻译成人话："从股票表里，挑出市盈率小于8的，把它们的名称和市盈率给我"。

**整个系统的核心使命，就是把你写的中文条件，自动拼出这种 SQL。**

### 2.4 DSL

DSL = **Domain-Specific Language（领域专用语言）**。

- 通用编程语言（Python、Java）语法复杂，什么都能干
- DSL 为某个特定领域设计，只干一件事，所以简单

本系统的 DSL 专门为"选股"设计，你只用学十几个字段名和几个运算符，就能写出选股条件。它比 SQL 简单得多，但又比"勾选下拉框"灵活得多。

### 2.5 API

API（应用程序接口）可以理解成"前后端约定的暗号"。

前端想查数据时，按约定好的格式发到**固定的地址**：
- 发到 `/api/dsl_filter`，带上一段 DSL 文本 → 后端就知道"这是要执行 DSL 查询"
- 发到 `/api/kline`，带上股票代码 → 后端就知道"这是要 K 线图数据"

这些固定地址就像餐厅菜单上的编号："服务员，来一份 3 号餐"——简洁无歧义。

---

## 3. 系统的五大组成部分

```
┌─────────────────────────────────────────────────────────────┐
│  ⑤ 前端页面（浏览器里看到的）                                │
│     index.html / detail.html / fundamental.html / ai_chat   │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP 请求（点按钮触发）
┌────────────────────────▼────────────────────────────────────┐
│  ④ Flask 后端（app.py）                                      │
│     接收请求 → 调度 → 返回结果                                │
└──┬──────────────────────┬───────────────────────────────────┘
   │                      │
   ▼                      ▼
┌──────────────────┐  ┌──────────────────────────────────────┐
│ ③ DSL 翻译引擎   │  │  其他业务逻辑                          │
│   dsl_engine.py  │  │  （筛选、K线、估值、AI 对话等）        │
│   中文→SQL       │  │                                       │
└────────┬─────────┘  └──────────────────────────────────────┘
         │ 生成 SQL
         ▼
┌──────────────────────────────────────────────────────────────┐
│ ② 数据库（DuckDB，instance/stock_data.duckdb）                │
│   5 张表：基本信息表 / 当日信息表 / 日度信息表 / 周度 / 月度  │
└──────────────────────────────────────────────────────────────┘
         ▲
         │ 写入数据
┌────────┴─────────────────────────────────────────────────────┐
│ ① 数据导入管道（update_db.py）                                │
│   从 Tushare API 拉数据 → 清洗 → 存进数据库                   │
└─────────────────────────────────────────────────────────────┘
```

下面五节**从下往上**（数据是自下而上流动的）逐块讲解。

---

## 第一部分：数据从哪里来——数据导入管道

**文件：[update_db.py](dsl-stock-screener/update_db.py) （约 800 行）**

### 这块是干什么的

数据库里的股票数据不会凭空冒出来。这块负责**从外部数据源（Tushare）把数据拉回来，整理好，塞进数据库**。它是整个系统的"进货渠道"。

### 数据源：Tushare

**Tushare** 是一个提供 A 股历史数据的第三方 API 服务。本系统通过它拉取：
- 所有上市股票的清单（名称、代码、行业、地域…）
- 每天的行情（开盘价、收盘价、最高最低、成交量…）
- 每天的估值指标（市盈率、市净率、总市值…）

调用 Tushare 需要 **Token**（一种身份密钥，类似账号密码）。本系统准备了多个 Token 轮换使用，因为免费额度有限，多几个能跑得快些。

### 它怎么拉数据：分表分批

`update_db.py` 把导入拆成 4 个子任务：

| 子任务函数 | 拉什么 | 怎么拉 |
|---|---|---|
| `update_basic_info` | 股票静态信息（名称、行业…） | 一次性全拉，整表替换 |
| `update_daily_snapshot` | 最新一天的行情+估值 | 只拉最新一天，整表替换 |
| `update_daily_history` | 历史每日行情（1800 万行！） | 按交易日批量拉，多线程并发 |
| `update_weekly_monthly` | 周 K、月 K | **不拉新数据**，用日度数据算出来 |

**为什么日度历史要 1800 万行？** A 股 5000 多只股票 × 30 多年 × 每年约 250 个交易日 ≈ 上千万行。所以这块最耗时。

### 关键设计：周/月数据是"算"出来的

第 4 个子任务 `update_weekly_monthly` **完全不调 Tushare**，而是用 SQL 从已有的"日度信息表"聚合出周线和月线：

```
日度信息表里每天一行 ──► 按周分组 ──► 开盘取周首、收盘取周末、高取最大、低取最小、量取总和 ──► 周度信息表
```

**好处：数据一致性**。只要日度数据准确，周/月数据自动准确，永远不会"日线和周线对不上"。

### 两种更新模式

| 模式 | 函数 | 行为 | 何时用 |
|---|---|---|---|
| **全量更新** | `full_update()` | 删掉所有旧数据，从 1990 年拉到今天 | 第一次建库，或数据坏了要重建 |
| **增量更新** | `incremental_update()` | 只拉"上次更新之后"的新交易日 | 日常每天/每周跑一次 |

增量更新很聪明：先查"日度信息表里最大的日期是哪天"，只拉那之后的交易日，重复的日子用 `ON CONFLICT DO NOTHING` 自动跳过。

### 防 Tushare 限速

多 Token 池 + 单 Token 1.2 秒间隔 + 三层重试（并发 → 5 轮 → 降速单线程），确保 1800 万行不漏。

---

## 第二部分：数据存在哪里——数据库

### 数据库里有 5 张表

| 表名 | 像什么 | 行数 | 主键 |
|---|---|---|---|
| **基本信息表** | 股票的"户口本" | 5,531 | 股票代码(ts_code) |
| **当日信息表** | 股票的"今日体检报告" | 5,526 | 股票代码 |
| **日度信息表** | 股票的"每日日记"（30年） | 18,030,339 | (股票代码, 日期) |
| **周度信息表** | 日记的"周报" | 3,812,243 | (股票代码, 日期) |
| **月度信息表** | 日记的"月报" | 907,112 | (股票代码, 日期) |

> **为什么表名用中文？** 有意为之——让不懂英文的业务人员一眼看懂表用途。列名用英文（如 `close`、`pe`），因为 SQL 和数据库软件对英文列名处理最稳。

### 三张表的关系

```
基本信息表               当日信息表               日度信息表
（每只股票 1 行）        （每只股票 1 行）       （每只股票 × 每天 1 行）
─────────────            ─────────────           ──────────────
ts_code  ◄──── 关联 ────► ts_code  ◄──── 关联 ────► ts_code
name                          close                    trade_date
industry                      pe                       open
area                          pb                       close
exchange                      total_mv                 high
list_date                     ...                      low
                                                       vol
                                                       ...
```

它们靠 **`ts_code`（股票代码，如 600519.SH）** 关联。核心设计思路：**把"不变的属性"和"每天变的数值"分表存**。

- "贵州茅台属于白酒行业" → 基本信息表（几个月不变）
- "贵州茅台今天收盘价 1700" → 当日/日度表（天天变）

### 表结构是配置驱动的

表结构定义在 **Excel 文件 `Duckdb数据库结构.xlsx`** 里：
- 每个 Sheet 对应一张表
- 每行一个字段（中文名、英文名、类型、是否写入）
- 只有"是否写入=是"的字段才进数据库

**好处：** 加新字段只改 Excel，不改代码。

---

## 第三部分：系统的核心大脑——DSL 翻译引擎

**文件：[dsl_engine.py](dsl-stock-screener/dsl_engine.py) （约 770 行）——竞赛核心交付物**

### 这块是干什么的

把人写的中文选股条件**自动翻译成数据库能执行的 SQL**。

```
输入（你写的 DSL）:
  MA(收盘价, 5) > MA(收盘价, 20) AND NOT ST

输出（系统生成的 SQL）:
  WITH latest AS (...), hist AS (...)
  SELECT ... FROM "基本信息表" b
  JOIN "当日信息表" s ON ...
  LEFT JOIN hist h ON ...
  WHERE ((h.f_avg_close_5 > h.f_avg_close_20) AND (NOT b."name" LIKE '%ST%'))
```

### 翻译流水线：五道工序

```
你的 DSL 文本
     │
     ▼  ① 词法分析（切块）
   Token 流（带类型的碎片）
     │
     ▼  ② 语法分析（搭骨架）
   AST（抽象语法树）
     │
     ▼  ③ 语义检查（查对错）
   类型检查通过
     │
     ▼  ④ SQL 生成（拼片段）
   SQL 片段 + 历史聚合列登记
     │
     ▼  ⑤ 组装（拼完整 SQL）
   可执行的 SELECT 语句
```

#### 工序 ① 词法分析（`_tokenize`，dsl_engine.py:191）

把源串切成 Token 流，每个 Token 带**字符位置**（用于后面报错定位）：

```
输入: MA(收盘价, 5) > MA(收盘价, 20) AND NOT ST

切出: IDENT 'MA' @0  OP '(' @2  IDENT '收盘价' @3  OP ',' @6
      NUMBER 5.0 @8  OP ')' @9  OP '>' @11
      IDENT 'MA' @13  ...  IDENT 'AND' @25  IDENT 'NOT' @29  IDENT 'ST' @33  EOF @35
```

这一步只管"切"，不管"对错"。`pos` 字段是后面"错误光标定位"的根基。

#### 工序 ② 语法分析（`_Parser`，dsl_engine.py:220）

用**递归下降**把 Token 流组装成 AST 树：

```
        AND（与）
       /      \
   > (比较)   NOT (非)     ← 左边"大于"，右边"非ST"
  /    \        \
call MA call MA  field(ST) ← 比较左右各是一个MA调用
 │arg   │arg
 ├收盘价 ├收盘价
 └5     └20
```

调用层次天然编码运算优先级：`parse_or → parse_and → parse_not → parse_comparison → parse_add → parse_mul`，越深的函数处理的优先级越高。

#### 工序 ③ 语义检查

语法对 ≠ 语义对。引擎给每个节点标记三种**类型**：

| 类型 | 含义 | 例子 |
|---|---|---|
| `number` | 数值 | 收盘价、市盈率、5 |
| `string` | 文本 | 行业、名称、"银行" |
| `bool` | 条件（布尔） | ST、市盈率 < 8 |

运算时检查：比较两边类型一致；AND/OR/NOT 操作数必须是 bool；算术两边必须是 number。类型不对抛 `DSLError`（带字符位置）。

#### 工序 ④ SQL 生成（`_Generator`，dsl_engine.py:363）

从 AST 叶子节点自底向上，**每个节点生成一段 SQL 片段**。

**叶子节点**：
- `field('收盘价')` → 查词库，对应 `s."close"`（"当日信息表"别名 s）
- `field('ST')` → `b."name" LIKE '%ST%'`（类型 bool）
- `num(5)` → `5`

**函数节点 `MA(收盘价, 5)`**——最精妙的设计：
1. 登记聚合列到内部字典：别名 `f_avg_close_5`，表达式 `AVG(CASE WHEN rn <= 5 THEN "close" END)`
2. 在 WHERE 里引用别名：`h."f_avg_close_5"`

同一窗口多次引用自动去重，`max_n` 收集用户用过的最大窗口，决定历史 CTE 的 `rn` 上限。

#### 工序 ⑤ 组装完整 SQL（`build_select`，dsl_engine.py:591）

三个 CTE 层层嵌套：

**CTE `latest`** — 锁定最新交易日（不硬编码日期）
```sql
SELECT MAX("trade_date") AS d FROM "当日信息表"
```

**CTE `hist`** — 时序函数降维（每只股票一行）
```sql
SELECT "ts_code",
       AVG(CASE WHEN rn <= 5  THEN "close" END) AS "f_avg_close_5",
       AVG(CASE WHEN rn <= 20 THEN "close" END) AS "f_avg_close_20"
FROM (
    SELECT ..., ROW_NUMBER() OVER (PARTITION BY "ts_code" ORDER BY "trade_date" DESC) AS rn
    FROM "日度信息表"
) ranked
WHERE rn <= 21
GROUP BY "ts_code"
```

**主查询** — 三表 JOIN：
```sql
SELECT b."name" AS name, ...
FROM "基本信息表" b
JOIN "当日信息表" s ON s."ts_code" = b."ts_code"
JOIN latest ON s."trade_date" = latest.d
LEFT JOIN hist h ON h."ts_code" = b."ts_code"
WHERE (编译出的条件)
ORDER BY b."symbol"
LIMIT 10000
```

> `hist` 用 `LEFT JOIN`：新股日度数据不足，MA 列为 NULL，WHERE 自动排除。对应"数据不足自动排除"的语义。

### DSL 词库

| 类别 | 数量 | 例子 |
|---|---|---|
| 数值字段 | 23 | 市盈率、收盘价、总市值、换手率…（带单位换算） |
| 文本字段 | 8 | 名称、代码、行业、地域、板块、交易所 |
| 布尔字段 | 5 | ST、ST股、风险警示、*ST、*ST股 |
| 历史函数 | 8 | MA、区间涨幅、N日最高/最低、连涨/连跌、创新高/低 |

### 错误处理：光标定位

`DSLError` 带 `pos`（字符位置），前端在错误处画 `^` 指示：

```
市盈率 <  AND 成交量 > 1000
        ^
这里不应该出现 'AND'，前面少了一个值
```

---

## 第四部分：系统的骨架——Flask 后端

**文件：[app.py](dsl-stock-screener/app.py) （约 2940 行）**

### 路由全览

按功能分组：

| 分类 | 路由 | 作用 |
|---|---|---|
| **页面类** | `/` | 主页（筛选页） |
| | `/detail` | K 线详情页 |
| | `/fundamental` | 基本面页 |
| | `/ai` | AI 对话页 |
| | `/toolbox` | 工具箱（占位） |
| **筛选类** | `/api/sector_list` | 取板块/行业/地域下拉列表 |
| | `/api/filter` | 表单筛选 |
| | `/api/dsl_filter` | **DSL 筛选（核心）** |
| | `/api/dsl_help` | DSL 语法帮助 |
| | `/api/export` | 导出 Excel |
| | `/api/update_db` | 触发数据库更新 |
| **行情类** | `/api/kline` | 取 K 线数据 |
| | `/api/fundamental_data` | 取基本面数据 |
| **AI 类** | `/ai/init` | 初始化 AI 会话 |
| | `/ai/chat` | 发送消息给 AI |
| | `/ai/clear` | 清空会话 |
| | `/ai/report` | 生成分析报告 |
| | `/ai/export_data` | 导出底层数据 |

### 核心业务函数

| 前缀 | 干什么 | 代表函数 |
|---|---|---|
| `Tool_function_*` | 工具函数（排序、日期转换、读密钥…） | `Tool_function_read_ai_key__` |
| `Tool_function_calculate_*` | 技术指标（MA/RSI/KDJ/MACD） | `Tool_function_calculate_ma__` |
| `Main_function_*` / `Main_Func_*` | 核心业务逻辑 | 见下 |

关键函数：
- **`Main_Func_Get_securities_identification_data__`**（app.py:441）：表单筛选核心，pandas 逐层过滤
- **`Main_function_apply_metric_filter__`**（app.py:653）：数值指标范围筛选
- **`Main_function_Get_security_histinfo_json__`**（app.py:744）：取历史 K 线数据（日/周/月）
- **`Tool_function_calculate_theoretical_valuation__`**（app.py:1814）：多模型估值（PE/PB/PS/PEG/DDM）
- **`Tool_function_create_ai_client__`**（app.py:2070）：通义千问 AI 客户端

### DSL 路由怎么调引擎

`/api/dsl_filter` 非常薄，几乎只做"接收 → 转发 → 返回"：

```python
@app.route('/api/dsl_filter', methods=['POST'])
def api_dsl_filter():
    data = request.get_json() or {}
    dsl_text = (data.get('dsl') or '').strip()
    dialect = DB_TYPE  # 强制跟随环境变量，防止方言错配
    try:
        result = execute_dsl(dsl_text, dialect=dialect)
        return jsonify({
            'success': True,
            'data': result['rows'],
            'sql': result['sql'],
            'dialect': result['dialect'],
            'log': log_messages
        })
    except DSLError as e:
        return jsonify({
            'success': False,
            'error': e.message,
            'error_pos': e.pos  # 前端画光标
        })
```

路由层只管运输，重活全在引擎——这是好的架构分层。

---

## 两条筛选管道：薄 SQL 厚 Python vs 厚 SQL 薄 Python

### 管道对比

| 维度 | 执行证券筛选（表单） | 执行指令（DSL） |
|---|---|---|
| **入口** | "执行证券筛选" 按钮 | "执行指令" 按钮 / Ctrl+Enter |
| **API** | `POST /api/filter` | `POST /api/dsl_filter` |
| **过滤位置** | **Python pandas**（全量拉取后逐层过滤） | **数据库引擎**（一条完整 SQL） |
| **查哪几张表** | 基本信息表 + 当日信息表（最新一天） | 基本信息表 + 当日信息表 + 可选日度信息表（历史） |
| **能查历史吗** | ❌ 不能 | ✅ 能（MA、创新高等时序条件） |
| **SQL 可见** | 无 | ✅ 前端展示翻译后的 SQL |

### 表单筛选：薄 SQL 厚 Python

```
① SELECT * FROM 基本信息表            → 5531 行全进 Python
② SELECT * FROM 当日信息表 WHERE...    → 5526 行全进 Python
③ merge 成 ~5500 行 DataFrame
④ pandas 逐层过滤:
     df[df['industry'].isin(...)]
     df[df['area'].isin(...)]
     df[df['name'].str.contains('ST')]
     df[col >= 下限]
```

数据先全拉进内存，再在 Python 里筛。

### DSL 筛选：厚 SQL 薄 Python

```
DSL 文本 → 编译器五道工序 → 生成一条完整 SQL
  （CTE + 窗口函数 + WHERE 全在 SQL 内）
  ↓
duckdb.execute(SQL)  →  在数据库引擎里完成全部计算
  ↓
只取最终结果行
```

Python 只做"把 SQL 发给数据库 → 接结果"，不参与任何过滤计算。

### 为什么会有两条管道

这是**两代筛选方式并存**：
- **表单筛选**是原型时代的产物——靠勾选下拉框，简单但死板，不能表达时序条件
- **DSL 筛选**是竞赛核心交付物——用文本写条件，灵活，能查历史，把逻辑推进数据库引擎

两者返回的行字典结构完全一致，所以前端 `renderTable()` 能同时渲染，用户看不出区别。**它们容易被误以为是同一条管道——表面统一，底下分叉。**

---

## 第五部分：用户看到的脸——前端页面

### 5 个页面

| 页面 | 文件 | 干什么 |
|---|---|---|
| **主页** | [index.html](dsl-stock-screener/templates/index.html) | 选股筛选（表单+DSL）、结果表格 |
| **K 线详情** | [detail.html](dsl-stock-screener/templates/detail.html) | 蜡烛图、技术指标图、时间轴滑块 |
| **基本面** | [fundamental.html](dsl-stock-screener/templates/fundamental.html) | 财报、估值、股东、公告 |
| **AI 对话** | [ai_chat.html](dsl-stock-screener/templates/ai_chat.html) | 和通义千问聊股票 |
| **工具箱** | [toolbox.html](dsl-stock-screener/templates/toolbox.html) | 占位，待开发 |

### 主页结构

**左栏（40% 宽）——条件输入区**：
1. 证券类型单选（股票/指数/ETF/LOF/国债/企债/可转债）
2. DSL 编辑面板（输入框、执行/语法帮助按钮、错误提示区、SQL 展示区、帮助面板）
3. 各类型对应的条件勾选块（交易所、行业、地域等下拉）
4. 数值指标范围输入（收盘价/市盈率/市净率/总市值/流通市值）
5. 过程日志区

**右栏（60% 宽）——结果展示区**：
- 结果数量 + 结果表格（双击行打开 K 线详情页）

### 前端怎么调后端（[script.js](dsl-stock-screener/static/js/script.js)）

以 DSL 执行为例：

```javascript
function executeDsl() {
    const dsl = codeInput.value.trim();
    fetch('/api/dsl_filter', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ dsl: dsl, dialect: 'duckdb' })
    })
    .then(r => r.json())
    .then(data => {
        if (data.success) {
            renderTable(data.data, data.type);
            showDslSql(data.sql, data.dialect);
        } else {
            showDslError(data.error, data.error_pos);
        }
    });
}
```

### K 线详情页的技术亮点（[detail.js](dsl-stock-screener/static/js/detail.js)）

约 960 行 JS，Canvas 手工绘制：
- 自实现技术指标：MA、RSI（Wilder 平滑）、KDJ、MACD（EMA12/26）
- 蜡烛红涨绿跌 + MA 均线叠加
- 副图可切换：成交量/成交额/RSI/KDJ/MACD
- 时间轴滑块拖拽缩放
- 鼠标十字光标 + 实时数值显示

---

## K 线详情与基本面管线

### K 线数据获取

```
用户双击筛选结果某行
  → /detail?code=000001&name=平安银行&type=股票&exchange=深证主板

detail.js: loadKlineData('101')     ← 默认日K
  → GET /api/kline?code=000001&exchange=深证主板&type=股票&ktype=101

后端 api_kline():
  ① Tool_function_build_ts_code__() → "000001.SZ"
  ② Tushare: pro.daily_basic(ts_code) → 换手率（K 线接口唯一实时调 Tushare 的部分）
  ③ 数据库查询（主体数据）:
      日K(101) → 日度信息表 WHERE ts_code=? ORDER BY date
      周K(102) → 周度信息表（预聚合，由 update_db.py 定期写入）
      月K(103) → 月度信息表
  ④ 合并换手率 → 返回 klines 字符串数组

前端:
  解析 → 计算全部技术指标 → 三层 Canvas 渲染
  (K线图 + 指标图 + 时间轴 + 副时间轴)
```

### 基本面数据管线

```
fundamental.html → POST /api/fundamental_data

后端串行调用 13 类 Tushare 接口（约 5-15 秒）:
  [1-2]  公司基础信息 + 详细信息（管理层/经营范围）
  [3-6]  财务数据四表 × 4 期（fina_indicator / income / balancesheet / cashflow）
  [7]    估值快照（daily_basic，回退 3 天找有数据的交易日）
  [8-9]  历史分红 + Shibor 无风险利率
  [10]   全行业 daily_basic → 行业平均 PE/PB/PS
  [11-12]股东户数 + 前十大股东
  [13]   最近公告

后端计算:
  缺失指标补算（周转率/周转天数/同比增长率）
  5 种模型理论估值（PE/PB/PS/PEG/股息贴现）+ 综合评级

前端 5 张卡片依次渲染:
  公司信息 → 财务信息（资产负债表/利润表/现金流量表 + 指标摘要）
        → 估值信息（行业对比 + 理论估值表）
        → 股东信息 → 公告信息
```

---

## 一次完整的运行：从开机到出结果

### 第 0 步：准备数据（一次性，或定期）

```
update_db.py 跑全量更新
  → 从 Tushare 拉 5000 只股票 × 30 年数据
  → 存进 DuckDB 文件（1.5GB）
  → 日度信息表 1800 万行就位 ✅
```

### 第 1 步：启动

```
terminal: python app.py
→ * Running on http://127.0.0.1:5000
Flask 就绪 ✅
```

### 第 2 步：用户打开浏览器

```
浏览器访问 http://127.0.0.1:5000
→ Flask 返回 index.html → 浏览器渲染主页
```

### 第 3-8 步：DSL 查询全流程

```
[浏览器]                    [Flask]                    [dsl_engine]                    [数据库]
    │                         │                           │                              │
    │ ①点"执行指令"            │                           │                              │
    │─ POST /api/dsl_filter ─→│ ②接收 + 转发              │                              │
    │                         │─ execute_dsl() ──────────→│                              │
    │                         │                           │ ③词法→语法→语义→SQL生成       │
    │                         │                           │─ 生成完整 SQL ──────────────→│
    │                         │                           │                              │④执行
    │                         │                           │←────────────── 结果行 ────────│
    │                         │←── {rows, sql, count} ────│                              │
    │←─ {data, sql, log} ────│                           │                              │
    │ ⑥ renderTable()        │                           │                              │
    │ ⑥ showDslSql()         │                           │                              │
    │ ⑥ appendLog()          │                           │                              │
    ▼                         ▼                           ▼                              ▼
 表格 + SQL展示 + 日志
```

用户看到结果。**从点按钮到看到结果，通常 2-5 秒**（纯当日指标查询秒回；带历史函数如 MA/创新高需 CTE 计算，稍慢但仍远快于 Python 全量过滤）。

---

## 数据库双后端架构

### 抽象层设计（[db.py](dsl-stock-screener/db.py)）

```
                    app.py / dsl_engine.py
                           │
                    db_connect()  ← 统一入口
                           │
                ┌──────────┴──────────┐
                │   STOCK_DB_TYPE      │
                └──────────┬──────────┘
                           │
           ┌───────────────┼───────────────┐
           │ duckdb        │            doris
           ▼               │                ▼
    duckdb.connect()       │    DBUtils.PooledDB (pymysql)
    conn.execute(sql,[])   │    cursor = conn.cursor()
    .fetchdf()             │    cursor.execute(sql, %s)
                           │    fetchdf(cursor) → DataFrame
```

### 方言差异矩阵

| 特性 | DuckDB | Doris |
|---|---|---|
| 连接方式 | 嵌入式文件打开 | TCP MySQL 协议 (FE:9030) |
| 表名 | `"基本信息表"`（中文+双引号） | `` `stock_basic` ``（英文+反引号） |
| 字段引号 | `"close"` | `` `close` `` |
| 占位符 | `?` | `%s` |
| 字符串拼接 | `a \|\| b` | `CONCAT(a, b)` |
| 连接池 | 不需要 | DBUtils PooledDB (max 8, min 2) |
| 数据量 | ~1.5GB 单文件 | BE mem_limit=50% (~4.9GB) |

### 切换方式

```bash
# DuckDB 模式（默认）
cd dsl-stock-screener && python app.py

# Doris 模式
export STOCK_DB_TYPE=doris
cd dsl-stock-screener && python app.py
```

### API 接口清单

| 方法 | 路径 | 功能 | 请求体 |
|---|---|---|---|
| GET | `/` | 筛选主页 | — |
| POST | `/api/sector_list` | 概念/行业/地域列表 | `{type: "行业列表"}` |
| POST | `/api/filter` | 表单筛选 | `{security_type, exchange, industry, ...}` |
| POST | `/api/dsl_filter` | DSL 选股 | `{dsl: "..."}` |
| POST | `/api/dsl_help` | DSL 语法帮助 | `{}` |
| POST | `/api/export` | 导出 Excel | `{data: [...], type}` |
| POST | `/api/update_db` | 更新数据库 | `{}` |
| GET | `/detail` | K 线详情页 | `?code=&name=&type=&exchange=` |
| GET | `/api/kline` | K 线数据 | `?code=&exchange=&type=&ktype=` |
| GET | `/fundamental` | 基本面页 | `?code=&name=&type=&exchange=` |
| POST | `/api/fundamental_data` | 基本面数据 | `{code, exchange, ts_code}` |
| GET | `/ai` | AI 对话页 | `?code=&name=&type=&exchange=` |
| POST | `/ai/init` | AI 初始化 | `{code, exchange, ...}` |
| POST | `/ai/chat` | AI 对话 | `{conversation_id, message}` |

### 页面间数据传递

通过 URL Query 参数 + Jinja2 模板注入：

```
index.html（筛选结果）
  └─ 双击行 → /detail?code=000001&name=平安银行&type=股票&exchange=深证主板
       ├─ "基本面信息" → /fundamental?code=...
       ├─ "AI 分析"    → /ai?code=...
       └─ "工具箱"    → /toolbox?code=...
```

---

## 常见疑问解答

**Q1：为什么数据要存本地，不直接查 Tushare？**
A：Tushare 是远程 API，每次调用慢且有额度限制。1800 万行历史数据，如果每次查询都现拉，可能要等几小时。本地数据库是"快"的根本。

**Q2：DuckDB 和 MySQL/Oracle 有什么区别？**
A：DuckDB 是"嵌入式"数据库，像打开一个 Excel 文件，不需要装服务器、不需要配置，一行 `import duckdb` 就能用。特别适合数据分析。并发写能力弱，但本系统是"读多写少"，正合适。

**Q3：DSL 和 SQL 到底什么区别？**
A：DSL 是给人看的（中文、字段名直观），SQL 是给数据库看的（英文、要懂表结构）。DSL 表达力比 SQL 弱（只能选股这一件事），但学起来简单十倍。本系统的价值就是**让人用简单的 DSL，享受 SQL 的性能**。

**Q4：为什么周/月数据要从日度数据算？**
A：Tushare 的周/月接口和日度接口数据口径可能不一致。本系统选"以日度为准"——单一数据源、单一真相，宁可多算，不可不一致。

**Q5：迁到 Doris 要改什么？**
A：架构已留好挂接点——DSL 引擎的方言层已定义 `DorisDialect`，`execute_dsl` 已支持 dialect 参数，只需补表名映射和引号差异，引擎主体不动。

**Q6：数据多久更新一次？**
A：日度数据每天收盘后跑一次增量更新即可。全量更新只在首次建库或数据损坏时跑。

**Q7：DSL 写错了怎么办？**
A：引擎在出错位置画 `^` 指示，并给出中文提示。点"语法帮助"可查全部字段名和示例。

**Q8：这个系统能用来真正炒股吗？**
A：**不能**。它是竞赛作品，数据有延迟，不实时，也没有交易接口。定位是"选股研究工具"——帮你从 5000 只股票里快速筛出符合条件的几十只，缩小研究范围。

---

## 附：技术栈速查表

| 层 | 技术 | 作用 |
|---|---|---|
| 后端语言 | Python 3.10+ | 全部后端逻辑 |
| Web 框架 | Flask | 路由、请求响应 |
| 数据库（开发） | DuckDB | 本地数据存储（1.5GB 单文件） |
| 数据库（生产） | Apache Doris 2.1.11 | 分布式 OLAP |
| DSL 引擎 | 自研（纯 Python） | 中文→SQL 翻译（五阶段流水线） |
| 数据源 | Tushare API | A 股历史数据 |
| 前端 | HTML + 原生 JS + Canvas | 页面与 K 线图 |
| AI | OpenAI SDK → 阿里云百炼 | 通义千问对话分析 |
| Excel 导出 | openpyxl | 结果导出 |
| 拼音处理 | pypinyin | 中文名排序 |

---

*本文档由 Claude 生成，合并自同级目录 `系统架构说明文档.md` 与 `系统运行原理.md`，覆盖 `app.py` / `dsl_engine.py` / `update_db.py` / `db.py` 及前端全部文件。*
